OCAD-kompatibelt banläggningsprogram. 

Se ASMapProvider.h för kontraktet mellan ASMapView och den klass som tillhandahåller kartan (bakgrundsbilden). Klassen bestäms av ASOcourseDocument. F.n. finns bara ASOCADController som map provider.

ASOCADController läser först in hela OCAD-filen (via ocdimport.c). Därefter går den igenom alla objekt i filen och skapar en cache innehållandes f.f.a. en CGPathRef. Det gör att själva uppritningen blir enkel. Cachen är sorterad i uppritningsordning (fungerar inte riktigt f.n.). Själva skapandet av cachen delas upp över flera tråda

I somras hittade jag purple-pen, som är ett open-source banläggningsprogram för PC skrivet i C#. Där kan man kika på hur den författaren tolkat vissa fält i OCAD.

